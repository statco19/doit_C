#include <stdio.h>

/*
void main(){
    
	int sum = 0, num = 1;
	while(num <= 5) {
		
		printf("num(%d) + sum(%d) = ", num, sum);
		sum += num;
		
		printf("%d\n", sum);
		num++;
	}
		printf("\nResult : num = %d, sum = %d", num, sum);
	
}
*/

// 1min quiz

void main() {
	
	int i = 0;
	
	while(i < 10) {
		puts("안녕하세요!");
		i++;
	}
}